// Package middleware — auth-service middleware.
// All context helpers (GetUserID, GetUserRole, GetUserEmail, RequireUser, NetworkIsolation)
// are defined in jwt.go within the same package.
// This file intentionally left minimal to avoid redeclaration errors.
package middleware
